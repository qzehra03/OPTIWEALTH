import { LoginForm } from "@/components/auth/LoginForm";

export const metadata = {
  title: "Login — OptiWealth",
  description: "Sign in to your OptiWealth financial dashboard",
};

export default function LoginPage() {
  return <LoginForm />;
}
