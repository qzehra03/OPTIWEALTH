import { DashboardView } from "@/components/dashboard/DashboardView";

export const metadata = {
  title: "Dashboard — OptiWealth",
};

export default function DashboardPage() {
  return <DashboardView />;
}
